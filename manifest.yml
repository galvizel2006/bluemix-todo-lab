declared-services:
  todo-db:
  label: cloudantNoSQLDB
  plan: Lite
applications:
- name:      bluemix-todo-node301017
  host:      bluemix-todo-node301017-${random-word}
  memory:    128M
  services:
  - todo-db 
